---
title: "[프로그래머스] 소수 만들기 (JavaScript)"
category: algorithm
date: 2022-07-04
author: Kihoon
---

## 문제

[코딩테스트 연습 - 소수 만들기](https://school.programmers.co.kr/learn/courses/30/lessons/12977)

<br />

## 코드

```jsx
function isPrime(n) {
  for (let i = 2; i <= Math.sqrt(n); i++) {
    if (n % i == 0) return false;
  }
  return true;
}

function solution(nums) {
  let answer = 0;
  for (let i = 0; i < nums.length; i++) {
    for (let j = i + 1; j < nums.length; j++) {
      for (let k = j + 1; k < nums.length; k++) {
        if (isPrime(nums[i] + nums[j] + nums[k])) answer++;
      }
    }
  }
  return answer;
}
```

<br />

## 문제 풀이 방법

주어진 숫자 중에서 3개의 수를 골라 더했을 때 소수가 되는 경우를 구하는 문제이다.

3중 for 문을 사용해서 3개의 수를 고르는 경우의 수를 구하고 소수 판별을 하였다.

<br />

## 새로 알게된 것

-
